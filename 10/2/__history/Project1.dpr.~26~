program Project1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var
  massiv : array[word] of integer;
  i,j,k : integer;
  s,ss : string;
  p,g : single;


begin
Randomize;

  for i := 1 to 10 do begin
    massiv[i] := (I + random(100))*2;
    s := s + FloatToStr(massiv[i])+' ';
  end;

writeln('Дан массив: [ ',s,']');
writeln('Его первый элемент рвен: ',massiv[1]{:3:0});


g := (massiv[2]/massiv[1]);
writeln(g);



readln;
end.
