program pogoda;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,System.MAth;

var
osadki:array[1..31] of integer;
i,j,k:integer;
s:string;
begin
for i := 1 to 31 do begin
  osadki[i]:=randomrange(0,10);
  s:= s + IntToStr(osadki[i]) + ' ';
end;

writeln('ќсадки за июль (в мм): ',s);



Readln;
end.
