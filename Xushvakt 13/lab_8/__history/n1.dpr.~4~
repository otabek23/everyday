program n1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var
i,n:integer;
s:string;
begin

for i := 20 to 40 do begin
  n:= n + i*i*i;
  s:= s + IntToStr(n) + '+';
end;
Delete(s,maxint,1);
writeln(s);


readln;
end.
