program n2;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var
i:integer;
a,b:real;
begin
write('Сколько элементов хотите ввести: ');
readln(n);

for i := 1 to n do begin
  Writeln('Введите ',i,' элемент');
  Readln(a);
  if (i mod 2=1) or (i=1) then b:= b+a;

end;
writeln('Сумма элементов стоящихся в нечётных местах ',b:10:2);
readln;
end.
